import 'package:flutter/material.dart';

class CampusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Campus'),
      ),
      body: Center(
        child: Text('Campus Screen'),
      ),
    );
  }
}
